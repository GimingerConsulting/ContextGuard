# Security

ContextGuard stores project metadata locally in `.contextguard/` and does not send telemetry or repository content to external services.

Report security issues through the GitHub repository. Avoid sharing private repository data in public issues.

ContextGuard must never alter Git history, delete source files, silently change models, enable MCP servers, or start subagents automatically.
