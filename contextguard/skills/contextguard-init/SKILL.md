---
name: contextguard-init
description: Initialize ContextGuard local project intelligence and managed guidance for the current project. Invoke explicitly with $contextguard-init.
---

# ContextGuard Init

Run the bundled local command:

```bash
contextguard init
```

Use the completion summary to report the project root, project kind, indexed file count and managed documents changed. Do not invent architecture details for empty projects.
