---
name: contextguard-uninstall-project
description: Explain and optionally remove ContextGuard project-local state. Invoke explicitly with $contextguard-uninstall-project.
---

# ContextGuard Uninstall Project

Run:

```bash
contextguard uninstall-project
```

Only remove `.contextguard` after explicit confirmation with `--yes`. Preserve user-authored content and never uninstall the global plugin.
